class Bike {
  brand: string;
  model: string;
  rim: string;
  rate: number;

  constructor(brand: string, model: string, rim: string, rate: number) {
    this.brand = brand;
    this.model = model;
    this.rim = rim;
    this.rate = rate;
  }
}

export default Bike;
